#include <string>
#include <vector>
#include <algorithm>

#include "edge.h"

using namespace std;

struct Automaton {

    public:

    private:
        vector < vector < pair <Edge, int> > > g;
        vector <bool> terminals;

};
